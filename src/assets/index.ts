/**
 * Asset exports and declarations
 * Place local images, icons, and SVG assets in subdirectories here.
 */
export const Images = {
  // e.g. logo: require('./images/logo.png'),
};

export const Icons = {
  // e.g. placeholder: require('./icons/placeholder.png'),
};
